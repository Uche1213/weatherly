# Weatherly

A polished, responsive weather dashboard built with React, TypeScript, Vite, and `lucide-react`.

## Run locally

```bash
npm install
npm run dev
```

Weatherly uses realistic local mock data for San Francisco, London, and Tokyo. No API key is required.
